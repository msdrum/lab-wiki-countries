import { Navbar, Container } from 'react-bootstrap';

function NavBar() {
  return (
    <div>
      <Navbar className="menu">
        <Container>WikiCountries</Container>
      </Navbar>
    </div>
  );
}

export default NavBar;
